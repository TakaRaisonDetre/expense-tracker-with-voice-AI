import {makeStyles} from '@material-ui/core'

export default makeStyles(()=>({
   income: {
       borderButton: '10px solid rgba(0,225,0,0,5)'
   },
   expense: {
    borderButton: '10px solid rgba(225,0,0,0,5)'
}
}));